{
  "name": "echostash-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "The universal prompt SDK - JavaScript and Python",
  "repository": {
    "type": "git",
    "url": "https://github.com/GoReal-AI/echostash-sdk.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
